# Chapter 7: Introduction to React

[🇪🇸 Ver en Español](#capítulo-7-introducción-a-react)

Part II begins - First React component for the CMS Dashboard using Vite and JSX.

## Files

| File | Description |
|------|-------------|
| `main.jsx` | React entry point (createRoot) |
| `App.jsx` | Root component with JSX demos |
| `App.css` | Application styles |
| `index.css` | Global styles |
| `components/Header.jsx` | CMS Dashboard Header component |
| `index.html` | Vite HTML entry point |
| `vite.config.js` | Vite configuration |
| `package.json` | Dependencies and scripts |

## Setup

```bash
cd part-2-react/chapter-07
npm install
```

## Run

```bash
npm run dev
```

## Concepts

- Vite project setup with React template
- JSX syntax (expressions, className, fragments)
- Functional components
- Component composition (import/export)
- Inline styles in React
- Hot Module Replacement (HMR)

---

# Capítulo 7: Introducción a React

[🇺🇸 View in English](#chapter-7-introduction-to-react)

Comienza la Parte II - Primer componente React para el CMS Dashboard usando Vite y JSX.

## Archivos

| Archivo | Descripción |
|---------|-------------|
| `main.jsx` | Punto de entrada de React (createRoot) |
| `App.jsx` | Componente raíz con demos de JSX |
| `App.css` | Estilos de la aplicación |
| `index.css` | Estilos globales |
| `components/Header.jsx` | Componente Header del CMS Dashboard |
| `index.html` | Punto de entrada HTML de Vite |
| `vite.config.js` | Configuración de Vite |
| `package.json` | Dependencias y scripts |

## Configuración

```bash
cd part-2-react/chapter-07
npm install
```

## Ejecutar

```bash
npm run dev
```

## Conceptos

- Setup de proyecto con Vite y template React
- Sintaxis JSX (expresiones, className, fragmentos)
- Componentes funcionales
- Composición de componentes (import/export)
- Estilos inline en React
- Hot Module Replacement (HMR)
